{
  "scripts": {
    "build": "npx tsc",
    "copyInputFiles": "cp -r input/ build/",
    "lint": "eslint src/ test/ --fix",
    "test": "npm run build && npm run copyInputFiles && node -r source-map-support/register --test"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@typescript-eslint/eslint-plugin": "^8.48.0",
    "@typescript-eslint/parser": "^8.48.0",
    "eslint": "^9.39.1",
    "source-map-support": "^0.5.21",
    "typescript": "^5.9.3"
  }
}
